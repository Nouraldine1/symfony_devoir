<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion Boutique{% endblock %}</title>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .bg-custom-blue { background-color: #1E90FF; }
        .hover\:bg-custom-blue:hover { background-color: #1C7ED6; }
        .sidebar { width: 18.5%; position: fixed; height: 100vh; }
        .content { margin-left: 20%; width: 80%; }
    </style>
</head>
<body class="min-h-screen flex flex-col">
    <div class="flex">
        <nav class="sidebar bg-gradient-to-r from-[#2b6cb0] to-[#1d263b] text-white p-6 space-y-6">
            <div class="text-2xl font-bold">Gestion Boutique</div>
            <ul class="space-y-4">
                <li><a href="{{ path('dashboard') }}" class="flex items-center hover:bg-gray-700 p-2 rounded">
                    <i class="fas fa-tachometer-alt mr-2"></i> Dashboard</a></li>
                <li><a href="{{ path('clients') }}" class="flex items-center hover:bg-gray-700 p-2 rounded">
                    <i class="fas fa-users mr-2"></i> Clients</a></li>
                <li><a href="{{ path('articles') }}" class="flex items-center hover:bg-gray-700 p-2 rounded">
                    <i class="fas fa-box mr-2"></i> Articles</a></li>
                <li><a href="{{ path('dettes') }}" class="flex items-center hover:bg-gray-700 p-2 rounded">
                    <i class="fas fa-file-invoice-dollar mr-2"></i> Dettes</a></li>
            </ul>
        </nav>

        <div class="content flex flex-col flex-grow">
            <header class="bg-white shadow p-4 flex justify-between items-center">
                <div class="flex items-center">
                    <input type="text" placeholder="Rechercher..." class="p-2 w-80 rounded-full border border-gray-300 focus:ring-2 focus:ring-blue-400 outline-none">
                </div>
                <div class="relative" x-data="{ open: false }">
                    <img @click="open = !open" src="https://via.placeholder.com/40" alt="Photo de profil" class="w-10 h-10 rounded-full cursor-pointer">
                    <div x-show="open" @click.outside="open = false" class="absolute right-0 mt-2 w-48 rounded-md shadow-lg py-1 bg-white ring-1 ring-black ring-opacity-5" role="menu">
                        <div class="px-4 py-2 text-gray-700">
                            <span class="block text-sm font-bold">{{ user.name }}</span>
                        </div>
                        <a href="{{ path('edit_profile') }}" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" role="menuitem">Modifier le nom</a>
                        <a href="{{ path('logout') }}" class="block px-4 py-2 text-sm text-red-700 hover:bg-gray-100" role="menuitem">Déconnexion</a>
                    </div>
                </div>
            </header>
            <main class="flex flex-grow p-6 bg-gray-50">
                {% block content %}{% endblock %}
            </main>
        </div>
    </div>
</body>
</html>
